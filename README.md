# Portfolio Website

This is my personal portfolio website, built with HTML, CSS, JavaScript, and Tailwind CSS (via CDN).

## Project Structure

- `index.html`: The main entry point.
- `style.css`: Custom styles and overrides.
- `script.js`: Interactive elements (mouse glow, scroll navigation).
- `logos/`: Images and assets.

